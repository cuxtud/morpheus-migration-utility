# PostgreSQL (recommended for production / fleet history)
# docker compose up -d postgres
DATABASE_URL=postgres://morpheus:morpheus@localhost:5432/morpheus_snapshot?sslmode=disable

# HTTP port (default 443 with self-signed TLS)
# PORT=8443
